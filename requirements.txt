litellm>=1.100.0
pyyaml>=6.0
rich>=13.0
keyring>=25.0
pytest>=8.0

# Optional: only needed for computer-use tools (see README.md "Computer use").
# pyautogui>=0.9.54
# pillow>=10.0

# Optional: only needed for `delphi chat --speak` and `delphi dashboard`
# (see README.md "Voice output"). Pulls in torch/torchaudio - a multi-GB install.
# chatterbox-tts>=0.1.0
# flask>=3.0
# torchcodec

# Optional: only needed for `delphi tray` and `delphi install-launcher`
# (see README.md "Running in the background").
# pystray>=0.19
# pillow>=10.0
# flask>=3.0
